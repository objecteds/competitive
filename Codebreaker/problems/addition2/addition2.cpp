#include <bits/stdc++.h>
int main(){
    int a, b; std::cin >> a >> b; std::cout << a+b;
}

// you can move your mouse and click on buttons without triggering the alert by going into inspect element (on firefox at least)