#include <bits/stdc++.h>
#define int long long
using namespace std;
void solve() {
    int x; cin >> x;
    x + 3 > 10 ? cout << "No\n" : cout << "Yes\n";
}

signed main() {
    int tc; cin >> tc;
    while (tc--) {
        solve();
    }
}