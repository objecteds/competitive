#include <bits/stdc++.h>
#define int long long
using namespace std;
void solve() {
    int n, m; cin >> n >> m;
    cout << n*2 + m*4 << '\n';
}

signed main() {
    int tc; cin >> tc;
    while (tc--) {
        solve();
    }
}