#include <bits/stdc++.h>
#define int long long
using namespace std;
void solve() {
    int x, y ; cin >> x >> y;
    cout << x*10+y*5 << '\n';
}

signed main() {
    int tc; cin >> tc;
    while (tc--) {
        solve();
    }
}