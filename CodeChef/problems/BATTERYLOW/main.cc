#include <bits/stdc++.h>
#define int long long
using namespace std;
void solve() {
    int x; cin >> x;
    x <= 15 ? cout << "Yes\n" : cout << "No\n";
}

signed main() {
    int tc; cin >> tc;
    while (tc--) {
        solve();
    }
}